import React from "react";
import ReactDOM from "react-dom";

const App = () => <h1>Hello from React + Jenkins + Docker + Kubernetes 🚀</h1>;

ReactDOM.render(<App />, document.getElementById("root"));
